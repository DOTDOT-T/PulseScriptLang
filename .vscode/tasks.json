{
    // See https://aka.ms/vscode/tasks.json for documentation
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake-build",
            "type": "shell",
            "dependsOn": "cmake-configure",
            "command": "cmake",
            "args": ["--build", "build", "--config", "Debug"],
            "group": "build",
            "problemMatcher": ["$msCompile"]
        }
    ]
}
